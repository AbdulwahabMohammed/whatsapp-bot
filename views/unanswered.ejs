<h1 class="mb-3">Unanswered Questions</h1>
<ul class="list-group">
  <% alerts.forEach(function(a){ %>
    <li class="list-group-item">
      <%= a.created_at.toISOString().slice(0,16).replace('T',' ') %> |
      <%= a.phone %> : <%= a.message %>
    </li>
  <% }); %>
</ul>
