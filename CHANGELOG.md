# سجل التغييرات

توثق هذه الصفحة جميع التعديلات التي تطرأ على المشروع.
يرجى إضافة كل تغيير جديد بترتيب زمني مع ذكر التاريخ ووصف مختصر.

## 2025-07-28
- الإصدار الأولي لإنشاء بوت واتساب يعتمد على OpenAI.

## 2025-07-28
- دعم تشغيل البوت لعدة منشآت وتوجيه الرسائل تلقائيًا.

## 2025-07-29
- إضافة واجهة إدارة ويب لإدارة المنشآت والمساعدين ورفع الملفات.

## 2025-07-30
- منع رفع مجلد logs وملفات auth-* بإضافتها إلى `.gitignore`.

## 2025-07-30
- توثيق وحدات `logger.js` و`metrics.js` و`queue.js` و`scheduler.js` في README والوثائق.
- إضافة متغير `NODE_ENV` إلى `docs/config.md`.
- تحديث `docs/api.md` بأمر `generate-faq` ومسار `/metrics`.
- تحديث `docs/architecture.md` لشرح الطابور والعامل والمجدول.

## 2025-07-31
- إضافة قسم "سيناريوهات متقدمة" في `docs/api.md` يشرح إدارة المنشآت المتعددة واستخدام لوحة التحكم.
- إنشاء اختبارات تكامل لمحاكاة تدفق رسائل واتساب وفشل واجهة OpenAI.

## 2025-08-01
- إضافة صفحة "Broadcast" في لوحة الإدارة لإرسال رسائل جماعية.
- إنشاء طابور جديد لمعالجة الرسائل الجماعية وتحديث العامل لدعمها.

## 2025-08-02
- توفير زر لتعطيل المصادقة الثنائية للمستخدمين وإضافة مسار API جديد لذلك.

## 2025-09-09
- معالجة أخطاء الاتصال بقاعدة البيانات في `src/index.js` مع تسجيل الرسائل والتتبع والخروج بكود خطأ.
- إضافة اختبار Jest يحاكي فشل الاتصال بقاعدة البيانات.

## 2025-09-20
- التحقق من وجود متغيرات PostgreSQL الضرورية قبل إنشاء تجمع الاتصالات وإلقاء خطأ واضح عند غياب أي منها.
- توثيق المتطلبات في `docs/config.md` وإضافة اختبارات وحدة للتهيئة الصحيحة والخاطئة.

## 2025-09-21
- تصحيح أمر `npm start` ليشغّل `src/index.js`، وتحديث الوثائق وإضافة اختبار تكامل للتحقق من الإعداد.

## 2025-09-22
- معالجة حذف متجر المتجهات عبر استرجاعه والتحقق من وجوده قبل رفع الملفات، مع إنشاء متجر جديد عند الحاجة وإضافة اختبار وحدة لذلك.

## 2025-09-23
- معالجة تعارض جلسة واتساب عبر إيقاف إعادة الاتصال وتحديث الحالة إلى `conflict` مع إرشاد المستخدم لحذف مجلد `auth-<botId>` وإعادة مسح QR.

## 2025-09-24
- تنظيف تلقائي لجلسات واتساب الفاسدة عند حدوث `SessionError` أو `Bad MAC` مع إيقاف البوت وإشعار المستخدم بإعادة مسح QR.

## 2025-09-25
- السماح بحفظ رسائل الوسائط الخالية من التعليق عبر استبدال النص الفارغ قبل الإدراج في جدول `messages` وتحديث اختبارات العامل لذلك.
- تحديث `initDb.js` لإزالة شرط عدم السماح بالقيم الفارغة عن عمود `text` وتعيين قيمة افتراضية فارغة لضمان التوافق مع الإصدارات السابقة.
- توثيق السلوك الجديد في الأسئلة الشائعة وإضافة اختبار يحاكي استقبال صورة بلا تعليق.

## 2025-09-26
- فرض حد أدنى لإصدار Redis (6.2.0) عند بدء العامل مع إيقاف التشغيل برسالة واضحة عند اكتشاف إصدار أقدم.
- تحديث README والوثائق لتوضيح المتطلب الجديد وتوفير أمر Docker يستخدم الصورة `redis:7-alpine`.
- إضافة اختبارات وحدة لوظيفة التحقق من نسخة Redis وتكييف اختبارات تدفق العامل مع الفحص المسبق.

## 2025-09-27
- فرض تعيين `SESSION_SECRET` وإيقاف تشغيل خادم الإدارة عند غيابه مع تفعيل سمات الأمان `HttpOnly` و`SameSite=Lax` و`Secure` لملفات تعريف الارتباط.
- تحديث `.env.example` وREADME والاختبارات للتأكيد على المتطلب الجديد ومنع التهيئة غير الآمنة.

## 2025-09-27
- تفعيل فلتر يحظر رفع ملفات تطابق تعليمات النظام وحجب أي رد يعيد تلك التعليمات للمستخدم،
  مع إضافة اختبار تكاملي وتحديث الوثائق لتنبيه الفرق إلى السلوك الجديد.

## 2025-09-28
- تفعيل حماية CSRF في لوحة الإدارة عبر مكتبة `csurf` مع تمرير الرمز إلى جميع القوالب.
- تحديث جميع النماذج لإرسال الحقل `_csrf`، وتعديل السكربتات لتحديث التوكن تلقائيًّا بعد كل طلب.
- تكييف اختبارات التكامل لإرسال رموز CSRF صالحة وتوثيق المتطلبات الجديدة في README و`docs/api.md`.

## 2025-09-29
- استبدال `process.exit(1)` في `src/openai.js` برمي استثناء واضح عند غياب أو عدم صلاحية المفتاح.
- تهيئة وحدات `chat` و`worker` و`assistant` وأداة رفع الملفات للتعامل مع فشل التهيئة عبر تسجيل الخطأ وتعطيل الاستدعاءات التي تعتمد على OpenAI.
- إضافة اختبارات وحدة تغطي حالات غياب المفتاح أو تمرير قيم غير صحيحة، وتحديث README و`docs/openai.md` لتوثيق السلوك الجديد.

## 2025-09-30
- إضافة سكربت `npm run check-env` للتحقق من اتصال PostgreSQL وRedis ومن وجود مجلدات جلسات واتساب قبل بدء العامل.
- استدعاء الفحص في `src/index.js` و`src/worker.js` مع إيقاف التشغيل برسالة واضحة عند فقدان أي اعتماد.
- تحديث README و`docs/setup.md` لشرح كيفية تشغيل الفحص الجديد، وإضافة اختبارات Jest تغطي حالات النجاح والإخفاق لفحص البيئة.
- تقييد الملفات المرفوعة للمعرفة بأنواع MIME وامتدادات معتمدة مع حد أقصى 10 ميغابايت لكل ملف، وإرجاع رسائل واضحة عند مخالفة الضوابط عبر CLI ولوحة الإدارة.
- تحديث لوحة الرفع والوثائق والاختبارات للتحقق من الحالات الصحيحة والخاطئة.

## 2025-10-01
- إضافة وضع `FAST_DEV` لتسريع الاختبارات المحلية عبر ضبط محاولات Polling وفترات الانتظار تلقائيًّا، مع إمكانية تخصيص القيم عبر متغيرات البيئة الجديدة `RUN_MAX_RETRIES` و`RUN_INITIAL_DELAY_MS` و`RUN_MAX_DELAY_MS` و`RUN_DELAY_GROWTH`.
- توفير خيار تعطيل كشف اللغة عبر `DETECT_LANGUAGE=false` مع الحفاظ على القيم الإنتاجية الافتراضية.
- جعل تأخير إعادة الاتصال بالواتساب والفاصل بين الرسائل الجماعية قابلين للضبط عبر `CONNECTION_RETRY_DELAY` و`BULK_MESSAGE_DELAY` مع قيم أخف في وضع التطوير السريع.
- تحديث الاختبارات للتأكد من احترام المتغيرات الجديدة وإضافة ملف `.env.dev.example` وتوثيق الإعدادات في README والوثائق.

## 2025-10-02
- تحويل مخرجات `src/logger.js` إلى صيغة JSON تتضمن الطابع الزمني والمستوى مع التقاط التتبعات للحفاظ على سجلات منظمة.
- إضافة نقل تدوير يومي يعتمد على `winston-daily-rotate-file` بحدود للحجم والاحتفاظ، مع كتابة الملفات `app-%DATE%.log` و`error-%DATE%.log` تلقائيًّا.
- تحديث README لتوثيق إعدادات السجلات الجديدة وإنشاء اختبار Jest يتحقق من البنية JSON ووجود النقل الدوار.

## 2025-10-03
- دمج إطار الترحيلات `node-pg-migrate` في سكربت `initDb.js` لضمان تنفيذ إنشاء الجداول داخل معاملات وإغلاق تجمع PostgreSQL بعد الانتهاء.
- تحديث README لشرح أوامر تشغيل الترحيلات وإبراز إمكانية تكرارها بأمان محليًّا أو عبر Docker.
- إضافة اختبار Jest يعتمد على `pg-mem` للتحقق من إمكانية تشغيل الترحيلات مرتين بدون أخطاء أو تغييرات مكررة في المخطط.

## 2025-10-04
- إضافة ترحيل `000002_schema_hardening` لتوفير أعمدة تتبع زمنية، فهارس فريدة، وروابط منشآت لكل من المحادثات، الوثائق، الإحصاءات، والأسئلة غير المجابة.
- تحديث وثيقة البنية لتوضيح تلبية احتياجات وحدات التصعيد، التحليلات، وإدارة المعرفة، وإضافة دليل مطورين يشرح تدفقات البيانات بعد التغييرات.
- توفير خطة تشغيل وتراجع للترحيل الجديد مع تعليمات التهيئة والنسخ الاحتياطية، وتحديث اختبارات Jest للتحقق من عدد الترحيلات والأعمدة المضافة.
