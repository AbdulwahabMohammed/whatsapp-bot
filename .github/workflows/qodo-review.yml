name: Save All PR Comments

on:
  pull_request:
    types: [submitted, edited]

jobs:
  save-comments:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: read
      contents: write

    steps:
      - name: Checkout repository on correct branch
        uses: actions/checkout@v3
        with:
          ref: ${{ github.head_ref }}

      - name: ⏳ Wait for any bots or reviewers to comment
        run: sleep 20

      - name: Append all PR comments to docs/review_notes.md
        uses: actions/github-script@v7
        with:
          github-token: ${{ secrets.GITHUB_TOKEN }}
          script: |
            const fs = require('fs');
            const path = require('path');

            const pr = context.payload.pull_request;
            const prNumber = pr.number;
            const prTitle = pr.title;
            const prBranch = pr.head.ref;
            const currentDate = new Date().toISOString().split('T')[0];

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: prNumber
            });

            let newContent = `\n\n---\n\n## 🧠 PR Comments (PR #${prNumber})\n`;
            newContent += `**Title**: ${prTitle}\n\n`;
            newContent += `**Branch**: \`${prBranch}\` &nbsp;&nbsp; 📅 **Date**: ${currentDate}\n\n`;

            if (comments.length === 0) {
              newContent += '_❌ No comments found on this PR._\n';
            } else {
              comments.forEach((c, i) => {
                const author = c.user?.login || 'Unknown';
                newContent += `### 💬 Comment ${i + 1} by \`${author}\`\n\n`;
                newContent += `${c.body}\n\n`;
                newContent += `🔗 [View in GitHub](${c.html_url})\n\n`;
                newContent += `---\n\n`;
              });
            }

            const filePath = path.join(process.cwd(), 'docs', 'review_notes.md');
            let existingContent = '';
            if (fs.existsSync(filePath)) {
              existingContent = fs.readFileSync(filePath, 'utf8');
            } else {
              if (!fs.existsSync('./docs')) fs.mkdirSync('./docs', { recursive: true });
            }

            const finalContent = existingContent + newContent;
            fs.writeFileSync(filePath, finalContent);

      - name: Commit and push all review notes
        run: |
          git config user.name "qodo-bot"
          git config user.email "qodo@users.noreply.github.com"
          git add docs/review_notes.md
          git commit -m "📝 Append all PR comments to review notes"
          git push origin HEAD:${{ github.head_ref }}
